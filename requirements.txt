# ─── Browser Automation & Stealth ───
# PENTING: Jangan upgrade playwright-stealth ke v2.x (breaking changes)
playwright==1.43.0
playwright-stealth==1.0.6
python-ghost-cursor==0.1.1
bezier==2024.6.20

# ─── HTTP & Web Scraping ───
requests>=2.32.5
duckduckgo-search>=5.3.0
beautifulsoup4>=4.14.3

# ─── System & Utilities ───
psutil>=7.2.2
python-dotenv>=1.2.1

# ─── Keamanan: Enkripsi vault kredensial ───
cryptography>=45.0.1

# ─── Web Dashboard ───
flask==3.0.2

# ─── Stealth Browser (lebih baik dari playwright-stealth biasa) ───
camoufox==0.4.11

# ─── HERMES AGENT ────────────────────────────────────────────────
# Hermes Agent (by NousResearch) — self-improving AI agent framework.
# Install dari GitHub (BUKAN dari PyPI):
#
#   pip install git+https://github.com/NousResearch/hermes-agent.git
#
# Hermes Agent menyediakan:
#   • Self-improving learning loop (auto-generates reusable skills)
#   • Persistent cross-session memory
#   • Multi-platform gateway (Telegram, Discord, Slack)
#   • Python-native AIAgent class
#
# Docs: https://github.com/NousResearch/hermes-agent
# ─────────────────────────────────────────────────────────────────
setuptools<70

# --- LLM Sandbox ---
# PENTING: Gunakan llm-sandbox[docker] bukan llm-sandbox saja
# llm-sandbox membutuhkan Python package 'docker' (Docker SDK) secara terpisah
# dari Docker Desktop. Tanpa ini muncul error:
# "Docker backend requires 'docker' package"
llm-sandbox[docker]>=0.1.0
docker>=7.0.0
mcp
browser-use>=0.1.0
langchain-google-genai>=1.0.0
